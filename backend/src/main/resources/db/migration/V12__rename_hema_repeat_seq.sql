-- =============================================================================
-- V12: Rename hema_repeat_seq to hema_seq to match BusinessIdGenerator convention
-- =============================================================================

ALTER SEQUENCE hema_repeat_seq RENAME TO hema_seq;
